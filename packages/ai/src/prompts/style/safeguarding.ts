// Safeguarding-guardrail prompt fragment.
//
// The product safeguarding workflow was removed in ADR 0013. The fragment is
// retained as an empty constant so existing prompt builders keep compiling
// and so a future reinstatement of the workflow has an obvious seam.
export const SAFEGUARDING_GUARDRAIL = ''
