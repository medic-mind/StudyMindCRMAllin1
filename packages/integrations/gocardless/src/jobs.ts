// Inngest functions triggered after a GoCardless webhook lands.
// See CLAUDE.md Section 17.

export const JOBS: readonly { id: string; description: string }[] = [
  // { id: 'gocardless/event.received', description: 'Process a GoCardless event from ProviderEvent' },
]
