// Inngest functions triggered after a Aircall webhook lands.
// See CLAUDE.md Section 17.

export const JOBS: readonly { id: string; description: string }[] = [
  // { id: 'aircall/event.received', description: 'Process a Aircall event from ProviderEvent' },
]
