// Inngest functions triggered after a Booking site webhook lands.
// See CLAUDE.md Section 17.

export const JOBS: readonly { id: string; description: string }[] = [
  // { id: 'booking/event.received', description: 'Process a Booking site event from ProviderEvent' },
]
