// Inngest functions triggered after a Stripe webhook lands.
// See CLAUDE.md Section 17.

export const JOBS: readonly { id: string; description: string }[] = [
  // { id: 'stripe/event.received', description: 'Process a Stripe event from ProviderEvent' },
]
