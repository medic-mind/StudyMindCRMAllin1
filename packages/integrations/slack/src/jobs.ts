// Inngest functions triggered after a Slack webhook lands.
// See CLAUDE.md Section 17.

export const JOBS: readonly { id: string; description: string }[] = [
  // { id: 'slack/event.received', description: 'Process a Slack event from ProviderEvent' },
]
